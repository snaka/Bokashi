/// Umbrella namespace. The app target's version comes from the bundle's
/// `CFBundleShortVersionString` (driven by `MARKETING_VERSION` in
/// `project.yml`) — not from here — so there is no constant to drift out
/// of sync with releases.
public enum BokashiCore {}
